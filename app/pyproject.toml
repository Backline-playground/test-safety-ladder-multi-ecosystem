[project]
name = "app"
version = "0.1.0"
description = "Minimal Flask app fixture for safety-ladder e2e testing"
requires-python = ">=3.9"
dependencies = [
    "werkzeug==3.1.0",
    "flask==3.1.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
